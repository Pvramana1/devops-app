stages:
  - build
  - test
  - deploy

variables:
  IMAGE: "$CI_REGISTRY_IMAGE/devops-app"
  DOCKER_HOST: tcp://docker:2375/
  DOCKER_TLS_CERTDIR: ""

# -------------------------
# BUILD STAGE
# -------------------------
build_job:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - echo "Building Docker image"
    - docker build -t $IMAGE:$CI_COMMIT_SHA .
    - docker save $IMAGE:$CI_COMMIT_SHA > image.tar
  artifacts:
    paths:
      - image.tar

# -------------------------
# TEST STAGE
# -------------------------
test_job:
  stage: test
  image: python:3.10
  script:
    - echo "Installing dependencies (if any)"
    - pip install -r requirements.txt || echo "No requirements.txt found"
    - echo "Running tests"
  cache:
    paths:
      - .cache/pip

# -------------------------
# DEPLOY STAGE
# -------------------------
deploy_job:
  stage: deploy
  image: google/cloud-sdk:latest
  script:
    - echo "Preparing deploy step"
    - echo "$GCP_KEY" > key.json || echo "No GCP key provided"
    - echo "Deploy skipped for now"
  only:
    - main
